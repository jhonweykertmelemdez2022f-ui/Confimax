# Archivo de ejemplo para secrets (no versionar en producción). Copia a .env y actualiza con valores seguros.
# PostgreSQL
POSTGRES_PASSWORD=changeme
POSTGRES_USER=confimax
POSTGRES_DB=confimax

# pgAdmin / conexiones
PGADMIN_PASSWORD=changeme

# Redis / otros si los usas en tu entorno
REDIS_PASSWORD=changeme

# MongoDB
MONGO_PASSWORD=changeme

# Otros secretos
JWT_SECRET=changeme-please-change
